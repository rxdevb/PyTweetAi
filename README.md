An agent that generates Python tweets based on the prompt and posts them to X.

## Setup
1. Clone the repo.
2. Create a virtual environment: `python -m venv venv`.
3. Install dependencies: `pip install -r requirements.txt`.
4. Create a `.env` file with your API keys (OpenAI & X).
5. Run the agent.
